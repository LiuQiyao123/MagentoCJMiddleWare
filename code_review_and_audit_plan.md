# 系统功能走查与审计计划

本文档旨在记录对 Magento-CJ 中台系统核心功能的全面代码审查（Code Review）和逻辑审计。目标是在进入正式测试阶段前，最大程度地发现潜在的 Bug、逻辑缺陷、性能瓶颈和安全隐患。

## 走查核心要求

每一次功能走查都必须遵循以下严谨标准：

1.  **API 合规性分析**:
    -   **接口确认**: 必须通过网络搜索或查阅官方文档，确认代码中使用的 API 端点（Endpoint）、请求方法（`GET`/`POST`）与最新的官方文档完全一致。
    -   **参数校验**: 逐一比对代码中构建的请求参数（结构、类型、必填项）是否完全符合 API 文档的定义。
    -   **响应解析**: 确认代码对 API 响应数据的解析逻辑（特别是嵌套结构和数据类型）是正确且健壮的。

2.  **业务逻辑与风险分析**:
    -   **逻辑完整性**: 审查代码是否完整覆盖了核心业务流程的所有分支和边界情况。
    -   **数据准确性**: 检查是否存在硬编码的“魔术数字”、虚拟数据（如假库存）或可能导致数据不一致的逻辑。
    -   **策略一致性**: 确认代码中的业务策略（如“最便宜”、“最快”）的实现方式与业务需求定义一致。
    -   **识别潜在风险**: 预测并识别出在特定条件下可能导致失败或非预期行为的逻辑点。

3.  **错误处理与健壮性**:
    -   **上游错误**: 审查当外部 API 调用失败（如网络错误、认证失败、服务器500）时，系统的处理方式是否合理，日志记录是否清晰。
    -   **数据源错误**: 审查当输入数据（如来自 Magento 的订单信息）不完整或格式错误时，系统能否优雅地失败并提供可供排查的错误信息。
    -   **边界情况**: 考虑 `null` 值、空列表、非预期数据类型等边界情况下的代码行为。

4.  **不确定性清单**:
    -   明确列出所有基于当前代码无法确定的、需要依赖外部 API 行为或业务决策才能明确的“不确定点”。这包括对 API 返回费用是否含税、多包裹如何处理等业务层面的疑问。

---

## 已完成的走查结论

### 1. 动态物流选择 (`app/services/order_sync.py`)

-   **结论**: 核心 `cheapest` 策略实现正确且健壮。
-   **已发现风险**:
    -   **[高风险]** `fastest` 策略的实现过于脆弱，强依赖 `logisticsAging` 的字符串格式，可能因格式变化而崩溃。
    -   **[中风险]** 对“最快”的理解可能存在偏差（取了时效下限而非上限）。
    -   **[中风险]** 当 `get_shipping_cost` API 调用失败时，日志信息会产生误导，错误地记录为“未找到可用物流方式”。

### 2. 单商品同步 (`app/services/product_sync.py`)

-   **结论**: MVP 级别的实现，仅能处理最简单的“单规格商品”，存在重大业务逻辑缺陷。
-   **已发现风险**:
    -   **[高风险 - Bug]** **多变体商品处理失败**：系统会将所有 CJ 商品都创建为 Magento 的“简单商品”，丢失所有颜色、尺寸等选项。
    -   **[高风险 - Bug]** **库存数据不准确**：系统为所有商品设置了 `100` 的虚拟库存，与 CJ 真实库存完全脱钩。
    -   **[中风险]** 定价策略不确定：当 CJ 价格为范围时，默认取**最低值**。
    -   **[已知取舍]** **图片处理被明确跳过**，所有同步的商品在 Magento 中都没有图片。

### 3. 订单同步与状态更新 (`app/services/order_sync.py`)

-   **结论**: **核心功能存在致命 Bug，状态回传链路中断。**
-   **已发现风险 (并已在紧急修复计划中修复)**:
    -   **[致命 Bug]** **状态更新功能完全失效**: `cj_client.py` 中缺失 `get_order_detail` (原为 `get_order_status`) 方法的实现。
    -   **[致命 Bug]** **物流同步流程错误**: 向 Magento 添加运单号未使用标准的“创建 Shipment -> 添加 Track”流程。
    -   **[中风险]** **同步流程脆弱**: 订单同步强依赖商品映射，任何一个 SKU 未映射都会导致整个订单失败，缺乏后续处理机制。

---

## 待执行的走查计划

以下是计划中剩余需要进行代码审查和逻辑审计的功能模块，请按照上述核心要求逐一进行。

### 1. 认证与授权 (Auth & Security)

-   **审查重点**:
    -   CJ Token 的获取、缓存、刷新和持久化逻辑 (`app/utils/token_manager.py`, `app/models/token.py`)。
    -   `_ensure_authenticated` 的重试和回退逻辑。
    -   中台自身 API 密钥的验证机制。
    -   密码哈希与数据加密的实现 (`app/core/security.py`)。

### 2. 后台任务与调度 (Scheduler & Queue)

-   **审查重点**:
    -   计划任务的健壮性 (`app/services/scheduler.py`)，特别是任务执行失败或超时的情况。
    -   Redis 队列操作的原子性与并发安全性 (`app/services/queue.py`)。
    -   Webhook 消费者 (`app/services/order_worker.py`) 的错误处理、重试与死信策略。

### 3. 配置与环境管理 (Configuration)

-   **审查重点**:
    -   `app/config/settings.py` 中配置的完整性与合理性。
    -   敏感信息（密钥、密码）的加载方式。
    -   数据库与 Redis 的连接池配置。

### 4. 日志与监控 (Logging & Monitoring)

-   **审查重点**:
    -   日志覆盖范围、信息丰富度 (`app/core/logging.py`)。
    -   Prometheus 指标采集的有效性 (`app/main.py`)。
